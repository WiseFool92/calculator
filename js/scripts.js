var add = function(number1, number2) {
    return number1 + number2;
  };
  
  var number1 = parseInt(prompt("Enter a number:"));
  var number2 = parseInt(prompt("Enter another number:"));
  
  alert(add(number1, number2));
  var add = function(number1, number2) {
    return number1 + number2;
  };
  
  var number1 = parseInt(prompt("Enter a number to add:"));
  var number2 = parseInt(prompt("Enter another number to add:"));
  
  alert(add(number1, number2));
  
  var multiply = function(number3, number4) {
    return number3 * number4;
  }
  
  var number3 = parseInt(prompt("Enter a number to multiply"));
  var number4 = parseInt(prompt("Enter another number to multiply by:"));
  
  alert(multiply(number3, number4));
  
  var divide = function(number5, number6) {
    return number5/number6;
  }
  
  var number5 = parseInt(prompt("Enter a number to divide"));
  var number6 = parseInt(prompt("Enter another number to divide by:"));
  
  alert(divide(number5, number6));
  
  var subtract = function(number7, number8) {
    return number7-number8;
  }
  
  alert(subtract(number7, number8));
  
  var number = parseInt(prompt("Enter a number to subtract"));
  var number4 = parseInt(prompt("Enter another number to subtract by:"));